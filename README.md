Перед запуском необходимо перейти по адресу:
https://my.telegram.org/
выбрать API development tools и записать в файл .env такие данные как:
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PHONE=
TG_CHANNEL_USERNAME=@ тут пишем название канала, за каторым следим (текст после https://t.me/ )


Перейди в свой проект на
👉 https://console.cloud.google.com/apis/credentials
Найди созданные OAuth 2.0 Client ID (тип — Desktop App).
Нажми на него → Download JSON.
Это и есть твой client_secrets.json.

Помести его в корень проекта, рядом с app.py, youtube_client.py, telegram_client.py,

После запуска бот откроет страницу авторизации в гугле, там нужно авторизоваться и выбрать свой канал на Ютуб

При повторном запуске мы увидим ошибку, в которой Google сообщает, что:

Твой бот (или приложение) использует OAuth 2.0 авторизацию для доступа к YouTube API.

Но проект ещё не прошёл проверку Google (OAuth Verification).

Поэтому им можно пользоваться только тестировщикам, добавленным вручную в консоли разработчика.

Как исправить? Добавить себя в тестировщики

Переходим в Google Cloud Console. Выбери свой проект, где подключён YouTube Data API.

Слева открываем: APIs & Services → OAuth consent screen. Ищем по вкладкам Test users (тестовые пользователи).

Жмем + Add users и добавляем туда свою Google-почту, через которую авторизовались в боте.

Сохраняем изменения.

Перезапускаем бота, снова выбераем канал и попробуем авторизоваться.

Только пользователи, указанные в этом списке, смогут пройти OAuth-авторизацию, пока проект не опубликован.

Создались файлы авторизации: в корне client_secrets.json, а так же сессия в папке data/sessions/telethon.session

